{
  "version": 2,
  "builds": [
    {
      "src": "ethers/app/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "build" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/ethers/app/$1" }
  ]
}
